init python:

    
    
    ############################ MID TONES ####################################
    
    def bleep001(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep001.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep002(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep002.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)           
            
            
    def bleep003(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep003.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep004(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep004.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep005(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep005.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep006(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep006.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep007(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep007.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep008(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep008.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep009(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep009.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep010(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep010.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            

    ############################ LOW TONES ####################################

    def bleep011(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep011.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep012(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep012.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)          
            
            
    def bleep013(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep013.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound")
            
            
    def bleep014(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep014.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep015(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep015.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep016(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep016.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep017(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep017.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep018(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep018.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep019(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep019.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep020(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep020.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)




    ############################ HIGH TONES ####################################

    def bleep021(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep021.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep022(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep022.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)          
            
            
    def bleep023(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep023.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep024(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep024.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep025(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep025.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep026(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep026.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep027(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep027.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep028(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep028.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
            
            
    def bleep029(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep029.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)

            
    def bleep030(event, **kwargs):
        if event == "show":
            renpy.music.play("audio/sfx/bleeps/bleep030.ogg", channel="sound", loop=True)
        elif event == "slow_done" or event == "end":
            renpy.music.stop(channel="sound", fadeout=1)
